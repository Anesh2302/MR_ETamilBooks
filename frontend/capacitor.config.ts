import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mretamilbooks.app',
  appName: 'MR ETamilBooks',
  webDir: 'out',
  server: {
    androidScheme: 'https',
    hostname: 'anesh2302.github.io',
  },
};

export default config;
